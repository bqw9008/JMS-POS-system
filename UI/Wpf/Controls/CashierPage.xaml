<UserControl x:Class="POS_system_cs.UI.Wpf.Controls.CashierPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Focusable="True"
             Background="#FAF8F1">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Margin="0,0,0,16" Padding="18" Background="#FCFAF4" BorderBrush="#DAE0D7" BorderThickness="1" CornerRadius="18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Margin="0,0,18,0">
                    <TextBlock x:Name="TitleText" FontSize="24" FontWeight="Bold" Foreground="#1F2A2E" />
                    <TextBlock x:Name="DescriptionText" Foreground="#5B6969" Margin="0,4,0,0" />
                </StackPanel>

                <TextBox x:Name="ProductInputTextBox"
                         Grid.Column="1"
                         MinWidth="300"
                         Height="46"
                         VerticalContentAlignment="Center"
                         FontSize="16"
                         Padding="14,0" />
                <Button x:Name="AddButton"
                        Grid.Column="2"
                        Height="42"
                        MinWidth="104"
                        Margin="10,0,0,0"
                        Padding="18,0"
                        BorderThickness="0"
                        Background="#52796F"
                        Foreground="#FCFAF4"
                        FontWeight="SemiBold"
                        Click="AddButton_Click" />
                <Button x:Name="ClearButton"
                        Grid.Column="3"
                        Height="42"
                        MinWidth="104"
                        Margin="10,0,0,0"
                        Padding="18,0"
                        BorderBrush="#DAE0D7"
                        BorderThickness="1"
                        Background="#ECF1E8"
                        Foreground="#1F2A2E"
                        Click="ClearButton_Click" />
            </Grid>
        </Border>

        <Border Grid.Row="1" Margin="0,0,0,16" Padding="18" Background="#FCFAF4" BorderBrush="#DAE0D7" BorderThickness="1" CornerRadius="18">
            <DataGrid x:Name="CartGrid"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      RowHeaderWidth="0"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      Background="#FCFAF4"
                      BorderThickness="0"
                      FontSize="14"
                      RowHeight="42"
                      ColumnHeaderHeight="40" />
        </Border>

        <Border Grid.Row="2" Padding="18" Background="#FCFAF4" BorderBrush="#DAE0D7" BorderThickness="1" CornerRadius="18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="140" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="DiscountLabel" VerticalAlignment="Center" Foreground="#5B6969" Margin="0,0,10,0" />
                <TextBox x:Name="DiscountTextBox"
                         Grid.Column="1"
                         Height="40"
                         VerticalContentAlignment="Center"
                         TextAlignment="Right"
                         Text="0.00"
                         Padding="10,0" />
                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="20,0,20,0">
                    <StackPanel Margin="20,0,0,0" VerticalAlignment="Center">
                        <TextBlock x:Name="TotalLabel" Foreground="#5B6969" HorizontalAlignment="Right" />
                        <TextBlock x:Name="TotalAmountText" Text="0.00" FontSize="24" FontWeight="Bold" Foreground="#1F2A2E" HorizontalAlignment="Right" />
                    </StackPanel>
                    <StackPanel Margin="20,0,0,0" VerticalAlignment="Center">
                        <TextBlock x:Name="PayableLabel" Foreground="#5B6969" HorizontalAlignment="Right" />
                        <TextBlock x:Name="PayableAmountText" Text="0.00" FontSize="24" FontWeight="Bold" Foreground="#1F2A2E" HorizontalAlignment="Right" />
                    </StackPanel>
                </StackPanel>
                <Button x:Name="CheckoutButton"
                        Grid.Column="3"
                        Height="46"
                        MinWidth="132"
                        Margin="10,0,0,0"
                        Padding="18,0"
                        BorderThickness="0"
                        Background="#52796F"
                        Foreground="#FCFAF4"
                        FontWeight="SemiBold"
                        Click="CheckoutButton_Click" />
            </Grid>
        </Border>

        <TextBlock x:Name="ShortcutHintText"
                   Grid.Row="3"
                   Foreground="#5B6969"
                   FontSize="12"
                   Margin="2,14,2,0"
                   TextWrapping="Wrap" />
    </Grid>
</UserControl>
